{
  "name": "kaleidoscope",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "wzrd src/app.js -- --debug",
    "build": "browserify -t uglifyify src/app.js -o dist/kal.js",
    "build-min": "browserify -t uglifyify src/app.js | uglifyjs --compress --mangle --output dist/kal.min.js -- ",
    "build-all": "npm run build && npm run build-min",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ramkumar Shankar",
  "license": "UNLICENSED",
  "dependencies": {
    "browserify": "^14.4.0",
    "pixi.js": "^4.5.6",
    "stats.js": "^0.17.0",
    "wzrd": "^1.5.0"
  },
  "devDependencies": {
    "uglify-js": "^3.1.2",
    "uglifyify": "^4.0.4"
  }
}
